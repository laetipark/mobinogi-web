import React from 'react';
import UserGuildManager from '../components/user-guild-manager';

const GuildManagementPage: React.FC = () => {
  return (
    <div className="guild-management-page">
      <UserGuildManager />
    </div>
  );
};

export default GuildManagementPage;
